# Work Log — deep-showdown Rust engine

A running log of the engine's development and its verified parity against Pokémon
Showdown (PS). Parity is measured by the differential harness (`crates/verify`): the
engine enumerates *every* outcome branch of a turn, and a turn "matches" if PS's actual
next state is a member of that set (relaxed to the mechanics currently modeled — HP,
status, active slot, boosts, hazards, weather). Representation fidelity (can the engine
losslessly hold a PS state) is tracked separately and has stayed at 100%.

Re-run the metric any time with:

```sh
cd showdown-rs
cargo run --release -p verify -- harness/traces/*.json   # read the AGGREGATE block
VERIFY_DEBUG=1 cargo run -p verify -- harness/traces/trace-seedN.json   # diagnose first mismatch
```

## Milestones

| Phase | Deliverable | Status |
|---|---|---|
| 1 | Workspace + flat `Copy` state (864 B) + id enums | ✅ |
| 2 | Reversible `Instruction` model + apply/reverse (700-case property test) | ✅ |
| 3 | JS trace generator — seeded, deterministic battles from real PS | ✅ |
| 4 | Differential runner — parse → project → diff (representation 100%) | ✅ |
| 5 | `generate_instructions` (turn engine) + mechanics | ◑ 92.2% turn parity |
| 6 | PS roll-emit patch for exact probability verification | ☐ pending |

## Turn-parity progression (phase 5)

Each fix was driven by a concrete trace divergence surfaced by `VERIFY_DEBUG`.

| Fix | Triggering evidence | Turn match rate |
|---|---|---|
| (baseline: switching, hazards, basic damage, status moves, end-of-turn) | — | ~15% |
| Choice-recording: separate forced switches from turn actions | Kingambit "fainted but switched" — forced switch clobbered the real action | ~40% |
| Exact 4096 fixed-point damage rounding (replaces float floor) | off-by-one HP on damage | 43% |
| Item modifiers: Choice Band/Specs ×1.5, Assault Vest, Life Orb ×1.3 + recoil | Dragapult Choice Band: atk 339 → 508 effective | 86% (seed1) |
| Protosynthesis / Quark Drive ×1.3 via `modify(5325, 4096)` | Earthquake dealing 295 not 246 (Booster Energy active) | 67% |
| Multi-hit independent rolls (Dragon Darts) + Purifying Salt (halves Ghost) | Dragon Darts producing odd totals; Shadow Ball not halved | 82% |
| Probabilistic target secondaries (Shadow Ball SpD / Thunderbolt par / Moonblast SpA) + Clear Body guard | Shadow Ball's 20% SpD drop unmodeled | 89% |
| Immunity-gated self-secondaries (Close Combat vs Ghost) | self −Def/−SpD applied on a 0× immune hit | 70% |
| Supreme Overlord (+10%/fallen ally), Toxic Debris, Headlong self-drop | various | — |
| U-turn mid-turn pivot (`generate_instructions_ex` takes pivot targets) | fast U-turn must switch before the opponent's move | — |
| **Offensive modifiers route by move category, not the stat read** | Body Press dealt 156 vs my max 141 | **92%** |

| Switch indexing: record switches by stable canonical index, not PS's reordered array position | `switch 3` resolved to a fainted mon; PS switched elsewhere | 93.3% |
| Skip end-of-turn residuals when the battle is over (a side wiped) | Garganacl healed +25 Leftovers on the winning turn; PS stops the turn | 95.9% |
| Ignore a fainted active's (stale) boosts in the comparison | engine kept boosts on a 0-HP active; PS reports 0 | 97.5% |

| Ordered replacement lists (U-turn pivot, then faint replacement, in order) | Dragapult pivots into Glimmora, Glimmora gets KO'd, p2 re-switches — 2 switches/turn | 98.4% |
| Per-hit crit (each multi-hit rolls crit independently) | Dragon Darts where one hit crit and one didn't — mixed outcome unreachable | 99.5% |

| Purifying Salt halves the attacker's *stat* (onSourceModifySpA), not final damage | ±1 hp on Shadow Ball vs Garganacl | 100% (120 games) |
| Skip a fainted Pokémon's status in the comparison (PS clears it on faint) | Thunderbolt paralyzed Gholdengo as it fainted; PS reports `none` | **100% (250 games)** |

Latest: **1558/1558 turns (100.0%) across 250 games**, representation 100%. The 2-team
gen9 slice is at full per-turn parity with Showdown.

> Rates are cumulative but not strictly monotonic per row: the exact-rounding change
> briefly *lowered* the rate by removing a lucky float-vs-fixed coincidence, which then
> exposed the Choice Band gap underneath it.

## Content architecture (in progress)

Move *effects* are now data-driven: `engine/src/data.rs` `MoveData` carries `self_boosts`,
`secondary_chance`/`secondary_boosts`/`secondary_status`, `status`, `side_condition`,
`heal`, `self_switch`, and `target_volatile`. `generate.rs` reads these instead of
matching individual move ids — so adding a move is adding a data row, and the data can be
generated from PS. The 2-team slice stays at 100% after this refactor (regression gate).

**Numeric ids + full generated tables (done).** `Species`/`MoveId` are now `u16` newtypes
indexed into `engine/src/gen.rs` — auto-generated by `harness/gen-data.mjs` from PS
(911 species, 954 moves), with `data::move_data`/`base_stats`/`species_types` as O(1)
array lookups and `from_id`/`to_id` as sorted binary search. `Ability`/`Item` stay curated
enums (they encode logic, not just data; unknown ids → `None` = no-op). The 2-team slice
remains 100% on generated data (one codegen bug found+fixed: Salt Cure's volatile is a 100%
`secondary.volatileStatus`, now folded into `target_volatile`). Next: random-battle traces
to surface unmodeled abilities/items.

## Widening to random battles

`gen-trace.mjs --format gen9randombattle` exercises the full roster (diverse species,
moves, abilities, items). Traces in `harness/traces-rand/`; verify the same way. Random
battles are the honest measure of *content* coverage — moves are fully data-driven, but
abilities/items are a curated set (unknown → no-op), so this is where the long tail shows.

Progression (40 random games, 1105 turns), each a broad data-driven addition:

| Added | Random match rate |
|---|---|
| (baseline: data-driven moves, no abilities/items beyond the curated slice) | 48.2% |
| Weather-setting moves + the 4 weather abilities + Sand/Snow defensive boosts | 49.5% |
| Intimidate (switch-in −1 atk), Levitate (Ground immunity) | 49.7% |
| Self-boosting status moves (Swords Dance, Dragon Dance, Calm Mind, ...) | (folded in) |
| Move recoil + drain (Brave Bird, Giga Drain, ...) | 53.8% |
| Ability batch: immunities (Flash Fire, Water/Volt Absorb, Sap Sipper, ...), offensive (Huge Power, Guts, Technician, Adaptability), defensive (Multiscale, Thick Fat, Ice Scales, Filter), Sturdy | 55.3% |
| Knock Off (item-conditional power), Pain Split, `Item::Other` (held-item presence) | 56.2% |
| Protect (blocks foe's move), Substitute (HP cost + damage routing) | 57.8% |
| Fold `secondary.self` boosts (Rapid Spin, Trailblaze, ...), Curse | 59.8% |
| Weight-based moves (Grass Knot, Low Kick, Heavy Slam, Heat Crash) | 61.0% |
| Forced-switch moves (Roar/Whirlwind/Dragon Tail) + self-switch status (Teleport/Chilly Reception/Parting Shot) | 61.1% |
| Sleep (fixed-counter wake) + Freeze can't-move | 62.3% |
| Move flags + flag-keyed abilities (Tough Claws/Sharpness/Iron Fist/Strong Jaw/Mega Launcher/Punk Rock), Soundproof/Bulletproof, contact recoil (Rocky Helmet/Rough Skin/Iron Barbs) | 62.7% |
| Focus Sash; status-immunity (Purifying Salt blanket, Good as Gold vs status moves) | **62.8%** (40-trace set) |
| Hustle (×1.5 physical atk), Unaware (ignore foe boosts), Serene Grace (×2 secondary chance) | — |
| **Crash fix: multi-hit branch explosion** — Population Bomb (`multihit: 10`) drove `32¹⁰ ≈ 1.1e15` per-hit branches (~1 EB), OOM-crashing the machine. High-hit moves now take a sumset-DP path enumerating distinct *total* damage (O(hits·HP·32), peak RSS ~19 MB). | **64.5%** (120-trace set) |

> Measurement set widened from 40 → 120 random traces here for a stable sub-0.5% signal, so
> the 62.8% → 64.5% step is partly the new, larger baseline rather than all new mechanics.
> The exact per-hit product is kept for ≤3 hits (`MAX_EXACT_HITS`), so Substitute/Sturdy
> interleaving on common 2–3-hit moves is unchanged; only fixed ≥4-hit moves take the DP path.

| Eviolite (×1.5 Def/SpD for not-fully-evolved; new SPECIES_NFE table) | 67.6% |
| Sitrus Berry (heal 1/4 at ≤1/2 HP); Defiant/Competitive (+2 on opponent drop); Moxie (+1 Atk on KO) | 67.9% |
| **Weather recovery** (Moonlight/Synthesis/Morning Sun/Shore Up) + **Rest** + **Strength Sap** — onHit-callback moves with no static `heal` field | 69.6% |
| Prankster (+1 priority to status moves) | 69.7% |
| **Fixed-damage moves** (Night Shade/Seismic Toss=level, Dragon Rage=40, Sonic Boom=20, Super Fang=½HP, Endeavor) | 70.0% |
| Contrary (inverts stat changes) | **70.2%** (120-trace set) |

### Goal: 100% random parity, prioritizing high-usage competitive mons

Trace set widened to **220** here (seeds 1–220) for a denser signal. The big remaining
gains came from *common end-of-turn residuals and move-execution volatiles* — not damage
modifiers — found by diagnosis (worst-trace / first-mismatch surveys) and by reasoning
about which mechanics touch a turn every time they're up.

| Added | Random match rate (220-trace set) |
|---|---|
| White Herb / Weakness Policy; contact-status (Flame Body/Static/Poison Point/Poison Touch) | 70.6% |
| Toxic Orb/Flame Orb (EoT self-status) + Chesto Berry | 71.1% |
| **Poison Heal + Magic Guard** (EoT residual; Gliscor/Clefable) | 73.6% |
| **Terrain** damage ×1.3 + Grassy Terrain EoT heal | 74.0% |
| **Screens** (Reflect/Light Screen/Aurora Veil, non-crit ½) + Infiltrator | 74.3% |
| Eviolite, Defiant/Competitive/Moxie, Justified, Liquid Ooze, Natural Cure, Lum Berry, Mold Breaker, type-boost abilities (Water Bubble/Transistor/…), Regenerator | 74.5–74.8% |
| **Forced-switch drag fan-out** (replay enumerates random drag targets — harness never logged them) | 74.8% |
| **Flinch** (Iron Head/Rock Slide/Fake Out/…, sequence-skip) + Inner Focus | 75.3% |
| **Leech Seed** EoT drain | 76.0% |
| Partially-trapped + Ghost Curse residuals; Speed Boost (EoT +1 Spe, switch-aware) | 76.7% |
| **Conditional base power** (Facade/Hex/Venoshock/Brine/Acrobatics ×2; Eruption/Water Spout = HP-scaled) | 77.2% |
| **Heavy-Duty Boots negate hazards** + Toxic Spikes/Sticky Web on entry + Magic Guard | **78.5%** |
| Rock Head/Scrappy/Unnerve; exact integer boost math; HJK crash; **confusion self-hit** | 78.8% |
| Switch-in/KO boosts (Intrepid Sword/Download/Beast Boost); sand-immunity + weather-heal abilities | 78.9% |
| **Full paralysis** (25% can't move) + **freeze thaw** (20%) | 80.8% |
| **Haze** + **Clear Smog** (reset stat stages) | **81.1%** |

> The move-execution volatiles/statuses were the richest vein: **full paralysis** alone was
> +137 turns (paralysis is everywhere), on top of flinch (+41) and confusion (+10). All are
> *safe to add* — they only expand the candidate set. From 62.8% (40-trace) to **81.1%**
> (220-trace) this session, slice held at 100% the whole way. Remaining tail: ±1 damage
> precision, faint/pivot-replacement `active_index` edges, Tera nuances, and the thinning
> rare-ability/item list (most top unmapped now don't affect membership: Loaded Dice covered
> by the multi-hit DP, Wide Lens/Light Clay don't change compared fields).

> Biggest single wins this stretch: **Heavy-Duty Boots** (ubiquitous — every HDB switch-in
> was over-damaged, +94 turns), **flinch** (+41), **Leech Seed** (+53), **Speed Boost** (+50),
> **Poison Heal + Magic Guard** (+94 over two metrics). Pattern holds: common *every-turn*
> residuals, *move-execution* volatiles (flinch/confusion), and *switch-in* effects (HDB,
> hazards, Regenerator) dominate; pure damage-modifier abilities are a thinner tail. Move-
> execution volatiles (flinch, confusion) and forced drags are *safe to add* — they only
> expand the candidate set, so the common path can't regress.

Methodology that worked: (1) **callback-implemented moves** carry no static data fields
(weather recovery, Rest, Strength Sap, fixed-damage) → hand-code by move id; (2)
**every-turn residuals** (Poison Heal, Leech Seed, Magic Guard, terrain/Grassy heal) and
**move-execution volatiles** (flinch) each touch many turns → high leverage; (3) the
**worst-trace survey** finds a single mechanic dominating one battle (e.g. Gliscor Poison
Heal); (4) some gaps are **harness-side** (forced drags weren't logged → enumerate them).

> **Crash fix** this stretch: Population Bomb (`multihit: 10`) drove the per-hit outcome
> enumeration to 32¹⁰ branches (~1 EB) and OOM-froze the machine. Multi-hit now uses a
> sumset-of-totals DP for >3 hits (and all variable [2,5] moves), enumerating distinct total
> damage instead of the per-hit product. The biggest parity gains came from *callback-
> implemented* moves (weather recovery, Rest, Strength Sap, fixed-damage) — these carry no
> static data fields, so the codegen can't see them; they must be hand-coded by move id.

The grind plateaus into the genuine Pokémon long tail: by ~63% the mismatch buckets are
noise-dominated (common moves co-occur with any failing turn), and each remaining mechanic
is individually rare (~0.1–0.5%). Known larger remaining items: variable multi-hit
(Icicle Spear/Bullet Seed 2–5 hits — exact membership needs a sumset-of-totals approach,
not per-hit-combo enumeration, which blows up past 3 hits); Wish/Future Sight (delayed,
and not captured in the trace projection); the ~100-ability/item logic tail. The
representation check is relaxed to modeled fields (hp/stats/status/types/boosts/hazards/
weather); ability/item/volatiles/tera are excluded.

The 2-team slice stays at **100%** throughout (regression gate). Remaining random-battle
gap is the classic Pokémon long tail, each diagnosable via `VERIFY_DEBUG=1`:
- **hp (~300)**: damage-affecting abilities (Multiscale, Thick Fat, Huge Power, Guts,
  Technician, Adaptability, Tinted Lens, Filter, type-immunity abilities like Flash Fire /
  Water Absorb / Volt Absorb / Sap Sipper), items (Eviolite, Expert Belt, berries,
  Rocky Helmet), variable multi-hit (2–5), fixed-damage moves (Seismic Toss).
- **boosts (~122)**: more secondary effects, ability-blocked drops, Contrary, etc.
- **active_index (~39)**: forced-switch moves (Roar / Whirlwind / Dragon Tail).
- **status (~28)**: sleep (can't act while asleep), freeze, status-immunity abilities.

Each is a self-contained data row or small hook; the harness names the culprit, so the
grind is mechanical. The representation check is relaxed to the modeled fields (hp, stats,
status, types, boosts, hazards, weather); ability/item/volatiles/tera are excluded.

## Key insight (worth not re-learning)

**Offensive damage modifiers run on the move *category*'s stat event** — `ModifyAtk` for
physical, `ModifySpA` for special — **regardless of `overrideOffensiveStat`**. PS reads
the Defense *value* for Body Press but resets `attackStat` to `atk`/`spa` before
`runEvent('Modify' + stat)` (`sim/battle-actions.ts:1709`). So Body Press (physical,
reads Def) IS boosted by an `atk` best-stat Protosynthesis, by Choice Band, etc. Compare
`proto_stat` to the category's offensive stat, not the stat actually read.

Also: PS's `chainModify([5325, 4096])` is modifier **5325**, not `13/10` (which truncates
to 5324) — a one-unit difference that causes off-by-ones on every Proto-boosted hit.

## Known remaining tail (~8%, diminishing returns)

Each is individually diagnosable via `VERIFY_DEBUG=1`.

- **±1 rounding** — PS's true damage occasionally lands exactly between two of the 16
  enumerated rolls (residual stat/boost-rounding gap vs PS's integer `calculateStat`).
- **active_index (~15 cases)** — fast-U-turn compounds and faint-replacement edges.
- **Compound Garganacl / Leftovers** cases and a few `status`.

Next leverage points if pushing higher: exact integer boost/stat rounding (kill the ±1s),
then the U-turn `active_index` compounds. True 100% ≈ reimplementing all of PS; the
harness provides the continuous metric to drive it.
