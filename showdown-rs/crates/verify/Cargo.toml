[package]
name = "verify"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[[bin]]
name = "verify"
path = "src/main.rs"

[dependencies]
engine = { path = "../engine" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
