# Deep-Showdown RL — Design Summary & Experiment Log

Living document: current recipe, design choices with their evidence, and a short log of every run.
Update per experiment (append a log row + amend the recipe when a lever is adopted).
Companion docs: `EVAL_RUBRIC.md` (behavior metrics & review protocol), `RESEARCH_PLAN.md` (long-term).

## Current recipe (as of night2)

**Task**: gen9randombattle vs mixed opponents, from-scratch PPO, no BC. North-star: heuristic win-rate
(SimpleHeuristics ≈ 0.98 vs random) with behavioral depth (EVAL_RUBRIC rows).

| component | choice | why (evidence) |
|---|---|---|
| **Obs (v5, 437 dims)** | public-info only; per-move: bp/acc/eff/est-damage/KO-flag/type-onehot/bench-eff + **mechanics** (is-phys, self-boost off/def/spe, inflicts-status, heal, priority); per-team-slot: hp/fainted/active + types + matchup scalars; globals: field/hazards + **timers**, incoming-threat from revealed foe moves, volatiles+counters both actives, unrevealed count | decision features ≫ identity embeddings at our sample scale; mechanics block gave 2× faster learning + first setup *conversion* (E12) |
| **Frames** | ×2 (prev obs appended after current; indices stay valid) | best late slope vs heuristic (D3: 0.76 rising); memory for the switching game |
| **Arch** | `setslot` ~262K params @ frames 2: trunk(240×2) over TRUNK_IDX subset (type one-hots excluded) + shared per-move scorer + shared per-switch scorer + DeepSets pooled slot context to trunk & scorers | slot-shared scorers make positional bias unrepresentable (flat MLP learned 63% slot-3 bias, plateaued 0.83 vs random); set-context required for setup to emerge at all (B: 0, C: 0.33/game) |
| **Reward** | ΔΦ per step + terminal ±20; Φ = 0.5·HPdiff + 1.5·faints (unknown foes = full HP). Boost/status Φ-terms now OPTIONAL/off (E3: wr-redundant post-features; weak unresolved edge on conversion quality) | single-potential elegance; reveal-artifact fixed |
| **Credit redistribution** | RETIRED from default (flag remains). E12 converted without it; persistence claim confounded with frames; highest-complexity component | complexity not earned (E3/E12 deconfounding) |
| **Curriculum** | frontier PFSP: episode weight ∝ base·(wr(1−wr)+0.1)², over random:1, maxbp:1, heuristic:2, self:0.5 (frozen snapshots q50) | from-scratch 0→0.46 in 22 min; hardest-first wastes episodes on unbeatable opponents cold |
| **PPO** | γ 0.99, λ 0.95, clip 0.2, lr 3e-4 const, ent 0.005–0.01 const (NO anneals — horizon-agnostic), epochs 6, minibatch 512, KL-early-stop 0.03, batch = 10 envs × 192 | env-bound 30:1 (t_collect 2.5s vs t_update 0.08s) → extra epochs free; entropy self-anneals 1.7→0.35; schedules overfit the horizon (Rob) |
| **Infra** | trainer-owned server recycled q40 updates; 10 thread-envs, batched forward; flake-rebuild; true resume; `--max-minutes`; wandb `deep-showdown` (all runs) | server rooms degrade sps 650→24 unrecycled; winning ends games faster (A 422 sps vs C 680) |

**Known gaps**: identity (species/ability/item) unmodeled; status usage 73% redundant; hazards/recovery
unlearned; no real recurrence (GRU is the next capacity step if frames plateau); imperfect-info belief.

## Experiment log

| run | Δ vs predecessor | heuristic wr | behavior/notes |
|---|---|---|---|
| *(pre-history)* flat MLP scratch | — | 0.83 **vs random** plateau | 63% slot-3 positional bias; Defog spam; probe-driven diagnosis |
| *(pre-history)* BC+value pretrain | imitation (now out of scope) | 0.985 vs random | proved capacity; established probe/rubric tooling |
| long-med (1.5M warm) | v2 obs, hard PFSP | 0.45 plateau | first mixed-opponent run; no forgetting |
| long-v3 (2.8M warm) | +v3 obs (damage calc, matchup) | 0.61–0.74 | +20pt step from matchup features; setup still 0.03/g |
| **A** (22min scratch) | frontier PFSP | 0.46 tail | from-scratch viable |
| **B** | +Φ boost/status terms | 0.56 | +0.10; setup still 0 (credit without representability) |
| **C** | +setslot set-context | **0.70** | +0.15; setup EMERGES 0.33/g — then extinct by 1M steps |
| final90 (annealed, 90min) | +anneals +self | 0.67 @1.2M | anneal exonerated for extinction; throughput confounded |
| deliverable (33min, killed) | C+self, const hparams | 0.63–0.65 @1.25M | setup extinct under const entropy too → economics, not schedule |
| **D1** | boost 0.3 | 0.60 | incentive size ≠ answer; recovery dabbling appears |
| **D3** | +frames 2 | **0.76 rising** | best late slope; memory pays vs switcher |
| **D4** | +redistribution | 0.64–0.70 | wr-neutral; setup persists 0.1/g @1M (vs 0); flow decays 5.8→1.0 |
| night1 (partial, 1.7M) | C+frames+redist+self | 0.65–0.75 | setup ALIVE 0.6/g @1.5M (6× deeper than ever); conversion still wasted 0.78 |
| **E12** (920k) | +move mechanics +field timers +volatiles (NOTE: ran WITHOUT redistribute) | 0.64 tail (2× faster early: 0.72 @210k) | **first CONVERSION**: 0% wasted, post-setup dmg 0.27 (prior ≤0.15), boostedKOs 0.45/g — achieved w/o redistribution → redistribution's unique value now unproven; hazards/recovery still 0 |
| night2 (4M) | full recombination: v5+frames+redist+self | **plateau ~0.70–0.75** from 1.4M→4M (close 0.75/0.77) | features×frames did NOT compound past the ceiling; BUG FOUND: ModelPlayer didn't frame-stack → self-play opponents played random (PFSP starved them to w0.02; fixed after) |
| **E3** (920k) | E12 minus Φ boost/status terms (Rob's ablation) | 0.68 tail (≈E12) | **Φ-terms wr-redundant post-features**; setup discovery survives (0.35/g); conversion sloppier (wasted 0.43 vs 0.0, n~5 — inconclusive). RULINGS: redistribution RETIRED (complexity high, unique value never survived deconfounding); Φ-terms optional/off by default |
| night2 snapshots | behavior-over-time probes @480k/1.9M/3.9M | — | setup 0.33→0.07→**0.47/g (wasted 0.14)**: emergence → dip → RE-EMERGENCE with conversion at depth; wr plateau ≠ behavioral stagnation; status play deleted entirely |
| night3 (stopped @2.4M) | elegant recipe: v5+frames, no Φ/redist, REAL self-play | 0.60→0.75, tail 0.73/0.62 | **real self-play verified in prod** (self wr 0.50, 32% of episodes); no plateau-break yet at 2.4M; superseded by night4 |
| **night4** (16M done; extended→27.6M, stopped) | + v6 verb block + v7 context/legality — obs 665, 280K params | flat ~0.70 from 0.6M→14M (powered: 0.717@6M, 0.690@14.2M), then **LATE STEP: 0.763±0.029 @16M; extension HELD the level: 0.746±0.038 @27.3M** (500 eps) | punctuated equilibrium real; behavior phase transition consolidated into a stable "boost-and-break sweeper" identity: setup 1.7/g both opponents, avg max boost 2.4, bKO 1.0/g vs heuristic, recovery integrated (0.4-0.85/g), **status redundancy 0.00 everywhere** (pathology resolved). Weaknesses: over-investment (wasted 0.5-0.7 — stacks boosts beyond conversion), hazards never learned (0.03), safe-setup drops to 0.69 vs heuristic. New plateau ~0.75; next levers: GRU, foul-play calibration, identity tier |
| **P0.1 calib** (eval only, 2026-07-11) | night4 vs **foul-play** (expectiminimax + set inference), 100ms search, via challenge/accept pairs on local PS (`scripts/fp_eval*.sh`) | **0.106 ± 0.035** (31W/262L, n=293; path validated: 0.70 vs heuristic n=30 same code) | the frontier calibration RESEARCH_PLAN P0.1 called for: 0.746-vs-heuristic is nowhere near the ceiling. Foul-play's edge = set inference + search ⇒ identity/belief load-bearing for the new policy, not accelerants. 2000ms full arm (n=150) in `runs/fp_eval/` |
| **scale2** (Rust-env era begins, 2026-07-27; ongoing) | `train_flow` on `FlowVec` decision points: 13 actions, real PS team pool, league PFSP (snapshots + Rust heuristic w4.0), Φ=HP+0.5·faints shaping 0.15, 1024×3 trunk, TF32+fused Adam, ~16k sps | 0.083@3.4M → 0.238@125M → late step ~170–400M (0.32 band → 0.43) → **~0.50@620M** vs heuristic (crossed even; peak 0.538, still rising) | throughput campaign: Rust heuristic port (was 83% of wall time), TF32, fused Adam, crash-proofed pause-resume; entropy 1.0@130M → 0.33@620M, declining slowly but PAID FOR throughout; night4-style punctuated equilibrium replicated (~180M flat at 0.32, then +13pts). NOTE: this era's heuristic eval is the FIXED port — numbers are not directly comparable to poke-env-era heuristic rows |
| **W1 P0a/P0b calib** (EXPLORATION_PLAN, 2026-07-27) | exploiter probes (10M best-response vs frozen target) + `mcts@100ms` ladder arm | exploiter vs scale2@152M: **0.235@10M** (rising, 0.10@4M); vs random anchor: 0.874@5M (sanity); scale2@152M vs mcts@100ms: **0.14** (n=200; 91.6ms vs 0.025ms per action) | exploitability metric calibrated: discriminates hard, league PPO not trivially exploitable at 10M-BR budget. Perfect-info MCTS crushes the policy — same shape as P0.1 → search is the dominant axis (E2 gate priority) |
| **W2 E1 BC-init** (2026-07-27) | BC on 2M Rust-heuristic decisions (holdout acc **0.51** vs deterministic teacher) → 10M PPO probe, scale2 recipe/seed | BC standalone: 0.113 vs heuristic; BC-init PPO: 0.073/0.066/0.123→**0.099@10M** ≈ control (0.083/0.093/0.095) | **KILLED (diagnosed)**: teacher conditions on hidden info (`state_json` perfect-info) our public-info obs can't express — 51% clone ceiling, so the warm start carries no signal PPO doesn't refind. Does NOT kill E5 (human actions are public-info-generated). Opens an obs-audit question: what PUBLIC info is the 643-dim encode missing? |
| **W2b E1 kickstart** (2026-07-27) | annealed distillation −log π(teacher) coef 0.5→0 by 5M, heuristic teacher, 10M probe, scale2 recipe/seed | 0.096/**0.130**/0.120 → **0.102@10M** ≈ control | **KILLED — E1 family closed**: transient mid-run lift (0.130@6.6M vs control 0.093, CIs overlap), gone by endpoint. The league already extracts what the heuristic knows; warm-starting from it does not pay at any arm. Next teacher-shaped bet is E5 (human replays) behind its gate |
| **W4 E4 R-NaD-lite** (2026-07-27) | r′ = r − η(log π − log π_ref) on acting steps, ref refreshed q50 updates, learner seat only; 10M probes η∈{0.05, 0.2} vs clean control, 5M exploiter probes on endpoints | heuristic@9.8M: control 0.122 / η05 0.113 / η20 **0.030**; exploiter@5M: control **0.508** / η05 0.545 / η20 0.631 | **KILLED**: exploitability got WORSE under regularization at both η, and η=0.2 strangles learning outright. Caveat: real R-NaD is NeuRD + both-seat regularization over long outer loops — this one-seat lite probe rules out the cheap win, not the method class |
| **W7 outcome-head + subgame search** (2026-07-28) | second value head on UNSHAPED outcome λ-returns (E2 branch-B fix); depth-2 search: policy-prior top-4 both seats, child matrix games SOLVED (regret matching) and game values backed up — every simultaneous node resolved by ~Nash, never "opponent plays π" (the Wang-thesis gap) | raw 0.090 vs heuristic; **d1 search vs raw 0.597** (E2 critic: 0.437, LOST); **d2 vs raw 0.874**; **d2 vs heuristic 0.420 @ 66ms/action**; d2 vs mcts@100ms 0.127 (matched time) | **GATE OPEN**: the value TARGET was the whole story — same trunk/policy/search code. A 10M-step policy + search ≈ a 400M-step raw policy vs heuristic. Outcome head ≈ free at training time (one scalar regression on existing GAE machinery); policy cost within control band (0.083–0.090 vs 0.095). Caveat: transitions are perfect-info (true engine state); W8 = realistic search via `State::observe` determinization — the engine's fog-of-war Reveal system already anticipates exactly this |
| **GOAL W10 honesty + features** (2026-07-28) | leak audit closed 4 foe-obs leaks (exact stats/spread, sleep counter, sub HP, raw HP totals) with public-info re-derivation; `tests/obs_honesty.rs` = scramble-invariance PROOF the obs is information-set-only; encode_v2 (+28 honest damage-calc feats); frames=2 lever | B1 (v2 feats): 0.046/0.103/**0.123** → 0.092@10M vs honest control 0.066 (**+3–6pts**); B2 (v2+frames): 0.082/0.083/0.119 → 0.076 (**neutral at 10M** — matches D3's late-slope-only history) | **v2 features ADOPTED; frames CARRIED on night-era long-horizon evidence** (cost small, 10M can't see late slope). scale2 RETIRED at 1.107B steps (0.55 vs heuristic, leaky-obs/2k-pool era) — final ckpt banked as FINAL-ckpt_001106771968.pt. Hero recipe frozen: fog + v2 + frames + outcome + 100k pool + league/heuristic |
| **W9b belief on 100k pool** (2026-07-28) | control (fog+outcome) vs +belief, both 10M on the fresh 100k pool; far-holdout (seed idx 500k+) generalization read; det-d2 regression | belief acc: **0.33% train / 0.24% far-holdout** — memorization gone, but ≈ marginal prior (no inference learned at 10M); belief arm policy **0.027–0.073 vs control 0.062–0.089** (real tax); 100k control 0.066@9.8M vs 2k-era 0.111 (**the 2k memorization subsidy ≈ 3–5pts**); det-d2 vs raw 0.887 (4th ckpt in a row ≥0.84) | **BELIEF HEAD KILLED for the hero recipe**: no signal beyond base rates + measurable policy cost. Determinization stays generator-prior (which IS the correct Bayesian prior absent action-inference). Revisits: tiny coef, posterior-quality metrics (not top-1), longer horizon, or E5 replay-trained. **100k pool ADOPTED** — honest curves from here |
| **W9 belief head** (2026-07-28) | predict hidden foe identity (6 species + active item/moves) from public obs; free labels from true state; CE on HIDDEN entries only; fog+outcome+belief 10M probe | in-training acc hit **0.98**… then the held-out test: **0.79 on training-pool teams, 0.0015 (=CHANCE) on 2k held-out generator teams**. Policy 0.073–0.083 (low edge of band); det-d2 search regression check fine (0.87 vs raw) | **INVALIDATED BY POOL MEMORIZATION — and the finding outranks the probe**: 2,000 fixed teams let ANY net infer a full roster from a revealed lead (lookup, not inference), partially defeating fog obs and possibly flattering every in-pool eval. Fix shipped: 100k-team pool generated from the pinned generator; hero run and W9b (belief re-probe) must train on it. Belief machinery is sound and waits on the bigger pool |
| **W8 fog + determinized search** (2026-07-28) | obs audit found `Reveal::SPECIES` never wired — every prior run saw the full foe roster from turn 0. Fixed opt-in (`--fog-species`, rides in ckpts); determinization resamples all viewer-unknowable foe attrs from pool donors (species clause, seen-mon battle state preserved); fog+outcome 10M probe, then perfect vs honest d2 search on its ckpt | fog policy 0.111 (≈ leaky control); perfect-info d2 vs raw **0.874** (E7 replicated); **DETERMINIZED d2 vs raw 0.841**; det d2 vs heuristic **0.450**; det d2 vs mcts 0.12@138ms | **HONESTY ≈ FREE: 96% of the search edge survives the information set.** Realistic search is deployable (revealed info + public generator prior only — ladder-legal). Hero-run design unblocked: fog obs + outcome head in training, determinized subgame search at play time. Refinements queued: belief head to sharpen the det posterior (action-conditioned inference the generator prior can't see), det-sample/time tuning |
| **W6 aux probe** (2026-07-27) | control recipe + `--aux` (opponent-move CE + action-conditioned world model), 10M; E2 search gate retested on the endpoint | heuristic: in-run 0.131@9.8M / endpoint 0.089 (300 fresh games) ≈ control band; search-gate retest: search vs raw **0.31** (still loses), search vs heuristic 0.113 | **NEUTRAL at 10M**: aux costs nothing but neither moves strength nor opens the search gate — prediction-shaped trunks don't fix a critic whose TARGET is wrong. Next value levers are target-level: value-as-evaluator (unshaped outcome, n-step) and E5 replay-grounded outcomes. Aux may still pay at long horizon; cheap to carry |
| **W5 E3 NFSP-lite** (2026-07-27) | average policy = BC (holdout acc 0.36 — stochastic evolving teacher) on a 1.5M-row algorithm-R reservoir of the BR run's own 9.0M acting decisions; judged by exploiter@5M | avg net: 0.910 vs random, 0.110 vs heuristic (≈BR 0.122, no crater); exploiter vs avg: **0.569** vs BR reference **0.508** | **KILLED**: the fictitious-play average is MORE exploitable than the final BR net, not less — uniform-over-history averaging of a policy that mostly improved monotonically just mixes in weaker past play. Real NFSP interleaves BR-vs-average DURING training; that coupling is what this lite probe skipped and what any revival would need |
| **W3 E2 value-search** (2026-07-27) | 1-ply expectiminimax over the learned value head (`lookahead_obs` engine clones, 13×13 regret-matching matrix game, ×2 samples, ~9ms/action), scale2@160M ckpt, zero training | raw vs heuristic **0.280**; search vs heuristic **0.262**; search vs RAW head-to-head **0.437** (search loses!); search vs mcts@100ms 0.035 (raw: 0.14) | **KILLED (branch B, diagnosed)**: the critic is a GAE baseline trained on shaped on-policy returns — asked to rank off-policy joint-action successors it is out-of-distribution and its noise beats the 1-ply signal. Search stays the dominant axis (mcts@100ms still crushes everything) but VALUE QUALITY is the prerequisite: aux heads (off since scale2 start), longer-horizon targets, replay-grounded values (E5). Search infra (Flow: Clone, lookahead_obs, matrix solver) is built and reusable the day the value head earns it |

## Decision principles (learned the hard way)
1. Plateau → behavior-probe BEFORE tuning (every real gain came from a diagnosed constraint).
2. Skills can emerge then go EXTINCT — probe along runs, not endpoints.
3. Single-variable probes at fixed STEP budgets (~920k) for attribution; combine only winners.
4. Judge levers on wr AND behavior; wr saturates and hides both gains and losses.
5. No horizon-tuned schedules; constant hparams, `--max-minutes` only as a stop.
6. Self-play steps can arrive VERY late (night4: +0.07 in the final 12% of a run after 8M flat
   steps) — don't certify an asymptote while the league is still hardening; check behavior
   metrics for phase-transition precursors before calling it.
7. Trust only powered evals (300+ eps); in-run n=60 readings are a ±0.10 instrument and WILL
   manufacture breakthroughs. Rolling train wr tracks the stochastic policy, not the greedy one.
